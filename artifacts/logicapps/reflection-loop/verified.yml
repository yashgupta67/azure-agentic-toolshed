status: preview # working | preview | broken-upstream | stale
verified_on: null
azure:
  service: logic-apps-standard
  sku: WS1
  transport: streamable-http
sdk:
  azure-openai: "check your deployed API version at build time"
sources:
  - url: https://learn.microsoft.com/en-us/azure/logic-apps/create-model-context-protocol-server-standard
    ms_date: 2026-02-25
    hash: null
hosts_tested:
  foundry: untested
  copilot-studio: untested
  vscode: untested
  claude-desktop: untested
notes: >
  Designed but not yet deployed. This one has a real (if small) per-call cost
  via Azure OpenAI token usage — note the actual model deployment used and a
  rough per-run cost once tested. Also confirm whether the Azure OpenAI
  built-in connector's action name is actually "Get Chat Completions" at the
  time you build this — connector surfaces shift and this hasn't been
  confirmed against a live designer session.
